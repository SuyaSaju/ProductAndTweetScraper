{
  "name": "infant-nutrition-product-scraper-cli",
  "version": "0.0.1",
  "description": "Scrape search results from retail sites and save them to a database",
  "main": "src/index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "branding": "node ./src/branding.js",
    "collect-tweets": "node ./src/collect-tweets.js",
    "verify-tweets": "node ./src/tweets-verification-api.js",
    "start:docker": "docker-compose up",
    "start:build": "docker-compose up --build",
    "lint": "standard \"src/**/*.js\"",
    "lint:fix": "standard \"src/**/*.js\" --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thomas Dornell",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "mongoose": "^5.9.2",
    "puppeteer": "2.1.1",
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "standard": "^14.3.1"
  }
}
